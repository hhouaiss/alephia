---
import { icons } from '../../utils/icons';
import type { IconName } from '../../utils/icons';

interface Props {
  name: IconName;
  class?: string;
}

const { name, class: className } = Astro.props;
---

<span class={className} set:html={icons[name]} />