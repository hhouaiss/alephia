---
interface Props {
  metric: string;
  description: string;
}

const { metric, description } = Astro.props;
---

<div class="bg-white p-8 rounded-xl shadow-lg text-center">
  <div class="text-4xl font-bold text-primary mb-4">{metric}</div>
  <p class="text-gray-600">{description}</p>
</div>